name: build-project
# run-name:
on:
  push:
    # Rien précisé donc tout

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - name: get-code
        uses: actions/checkout@v4

      - name: compile-project
        run: make